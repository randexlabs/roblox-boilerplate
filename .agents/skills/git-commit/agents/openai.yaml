interface:
  display_name: "Git Commit"
  short_description: "Write or create Conventional Commit messages"
  default_prompt: "Use $git-commit to inspect these changes and produce the right Conventional Commit message, creating a scoped commit when paths are provided."
