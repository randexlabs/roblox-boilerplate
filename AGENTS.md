# AGENTS.md Instructions

- Do not criticize or suggest changing the `src/StarterPlayerScripts` mapping in `default.project.json`.
- Do not say that mounting `src/StarterPlayerScripts` under `ReplicatedStorage.client` prevents scripts from running.
- This mapping is intentional in this project. Treat it as expected behavior.
- When discussing client scripts, assume the project already handles their execution.
