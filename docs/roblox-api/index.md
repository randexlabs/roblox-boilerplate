# Roblox API

- `instance-querying.md`: filtered Instance tree search guidance; consult when selecting descendants by class, tag, name, property, attribute, or hierarchy
- `temporary-state.md`: temporary character state guidance; consult when choosing between plain tables, attributes, value objects, instances, or remotes for short-lived gameplay state
