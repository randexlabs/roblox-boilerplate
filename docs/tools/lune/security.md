# Security

Lune scripts have full access to the machine.

Treat untrusted scripts as unsafe.

Safer options:

- run in a container
- run in a virtual machine
- use a sandbox wrapper for inspection, not as a primary security boundary
