# Conventional Commits format

## Structure

```text
<type>[optional scope]: <description>

[optional body]

[optional footer]
```

## Notes

- `type` is required (e.g. `feat`, `fix`, `docs`).
- `scope` is optional and should be a short noun (e.g. `docs(luau): ...`).
- Keep the description short and imperative.

